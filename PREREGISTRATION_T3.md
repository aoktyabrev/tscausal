# PREREGISTRATION — T3.0 (три стороны без селекции)

Запечатано до первого прогона: `scripts/t3_terms.py` написан, но не запускался; остальной код T3 не написан.
Цитаты — SOURCES.md (BW16, WBO23, BCRWZ19, ABCFGB15), выводы D10, D11.

## Определения

- **ISO_N** (D10) = W, нормированные для всех прямых инструментов (TF_N, класс OCB) **и** для всех обратных
  (TB_N). Нуль-пространство считается буквально: строки — разности `Tr[W ⊗ M_X]` для случайных произведений
  локальных CPTP-чои (прямых или обратных), кубиты.
- **Правило (вывод, до расчёта):** неединичный член Паули допустим в TF_N ⇔ есть сторона, у которой он «только
  вход»; в TB_N ⇔ есть сторона «только выход»; в ISO_N — оба условия. Счёт: TF_N = 1 + 16^N − 13^N,
  ISO_N = 1 + 16^N − 2·13^N + 10^N. N = 2: 88 и 19 (совпадает со Stage C); N = 3: 1900 и **703**.
- **Игра** (D11): игра BW16, пример 2 (доходы a, b, c равномерны; исходы x, y, z — догадки); граница 3/4.
  Вероятности `p = (1/8) Tr[W (M_A ⊗ M_B ⊗ M_C)]` (обобщение D5). Операции — TS; классически — биекции
  (доход, вход) ↔ (исход, выход), 24 на сторону, перебор 24³.
- **Причинная упорядоченность классического процесса** — по BCRWZ19 (сноска стр. 280), расширенная
  динамическим порядком: первая сторона получает константу, вторая — функцию выхода первой (её выбор может
  зависеть от этого выхода), третья — функцию выходов первых двух. **Причинно разделимый** классический
  процесс — выпуклая комбинация таких функций; проверка — LP.
- **Допустимость детерминированного процесса** — BCRWZ19: w∘f имеет неподвижную точку для всех 4³ наборов
  локальных функций; проверяется «ровно одна».

## План
- **T.0:** размерности TF_3, TB_3, ISO_3 (и калибровка N = 2 → 19); совпадение с правилом; число
  антикоммутирующих пар; члены по числу сторон.
- **T.1:** (а) Лугано (P = 000) с TS-операциями: игра BW16 = 1 (калибровка), W_AF ∈ ISO_3? (б) каноническое
  XOR-расширение WBO23, P равномерно, F сброшено: W̄, принадлежность ISO_3, значение игры, разделимость;
  (в) все обратимые расширения с однобитовыми P и F (минимальная размерность: у Лугано есть пара выходов
  с одним образом): перебор второго отображения ω₁ с нужными кратностями образов, отбор допустимых ω₁,
  затем режим (б) для ½(ω₀ + ω₁).
- **T.2 (классика):** многогранник классических ISO_3-процессов (диагональные W, 18 типов + положительность),
  его вершины (cdd.gmp). Для каждой вершины: причинная разделимость (LP) и максимум игры BW16 по TS-биекциям.
- **T.2 (квант):** see-saw по ISO_3 (кубиты, W 64×64, 702 параметра) на игре BW16 с TS-инструментами.
  Калибровка: тот же see-saw в классе TF_3 (OCB) обязан дойти до 1 (Лугано). Нарушения — только после
  пересчёта в mpmath.

## Исходы (из промпта)
- D6 обобщается аналитически → «запрет при любом N»;
- найден процесс без селекции с нарушением → «запрет только для двух сторон»;
- ни того, ни другого → фиксируется, что блокирует каждое, числами.

## Прогнозы

**Архитектор:**
- T.0: появятся члены с пересекающимися носителями, доказательство D6 не переносится буквально — средняя-высокая;
- T.1: в источниках глобальное прошлое P фиксировано (предселекция) — средняя;
- T.1(б): при случайном P нарушение Лугано исчезает — средняя (главный вопрос);
- T.2: хотя бы один процесс без селекции нарушает трёхстороннее причинное неравенство — низкая.

**Исполнитель:**

| пункт | прогноз | уверенность |
|---|---|---|
| T.0 | N = 2: 19 (TF 88); N = 3: TF = TB = 1900, ISO = 703; правило = нуль-пространство | 0.9 |
| T.0 | есть антикоммутирующие пары и трёхчастичные члены → доказательство D6 не переносится буквально | 0.95 |
| T.1(а) | Лугано с TS-операциями даёт 1; W_AF ∉ ISO_3 | 0.9 |
| T.1 | источники: P фиксировано (|000⟩) — уже подтверждено литчеком до запечатывания | — |
| T.1(б) | каноническое XOR-расширение при равномерном P: W̄ = (1/8)·1 точно (XOR с равномерным p рандомизирует входы); игра ≤ 3/4 | 0.97 |
| T.1(в) | однобитовые расширения существуют; при равномерном P у всех значение ≤ 3/4 | 0.6 |
| T.2 кл. | все вершины классического ISO_3 причинно разделимы | 0.55 |
| T.2 кл. | ни одна вершина не превышает 3/4 в игре BW16 | 0.65 |
| T.2 кв. | see-saw в ISO_3 не превышает 3/4 | 0.65 |
| итог | исход «ни доказательства, ни свидетеля» | 0.5 |
