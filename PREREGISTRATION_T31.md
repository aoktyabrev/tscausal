# PREREGISTRATION — T3.1 (проверка и структура свидетеля W*)

Запечатано до первого прогона T3.1 и **до получения результатов литчека** (T3.1.a запущен субагентом,
его отчёт ещё не прочитан). Код T3.1 не написан. Цитаты — SOURCES.md; D10 (ISO_N) — наше **определение**.

## Определения
- Процесс — стохастическая матрица T[i, o] (входы i, выходы o, по 3 бита). W* = ½(ω_L + ω̄_L), ω̄_L(o) = ¬ω_L(¬o).
- **Группа G** (T3.1.c): инверсии каждого из 6 проводов (вход и выход каждой стороны) — Z₂⁶; перестановки
  сторон — S₃; обмен вход↔выход у всех сторон одновременно (транспонирование T) — Z₂; |G| = 64·6·2 = 768.
  Действие: T' = T ∘ g. Обращение времени MH (MH-30) включает ещё и обращение порядка сторон, оно
  покрывается композицией с перестановкой.
- **Симметризуемость** процесса ω через g: ½(ω + g·ω) ∈ ISO₃ (точная проверка Уолша по правилу T.0) **и**
  причинно неразделима (точная LP cdd.gmp по 680 причинно упорядоченным функциям; для внешних точек —
  точный сертификат). Дополнительно: корреляция со стратегией «переслать» вне причинного многогранника
  (138 304 стратегии, HiGHS + точный сертификат).
- **Орбитальные смеси:** среднее по орбите ω под циклическими подгруппами ⟨g⟩ и под Z₂⁶, S₃, G.
- **T3.1.d:** все детерминированные функции процесса. Необходимое условие (довод «дедушки»): вход стороны не
  зависит от её собственного выхода, иначе локальная функция создаёт противоречие. Кандидатов 16³ = 4096.
  Допустимость — критерий BCRWZ19 (неподвижная точка для всех 64 наборов локальных функций). Причинность
  детерминированной функции — принадлежность множеству 680 упорядоченных функций (детерминированная точка
  разделима тогда и только тогда, когда совпадает с одной из них).
- **T3.1.b:** значения BW16 (пример 2) и G* с TS-стратегией «переслать» и с максимумом по 24³ TS-тройкам
  на ω_L, ω̄_L, W*; инвариантность игр относительно полной инверсии; неравенства I₁…I₄ Abbott et al.
  (1608.01528) — если цитаты их форм получены литчеком; иначе только BW16 и G*.
- **T3.1.e:** N = 4, обобщение Лугано из литературы (цитата от субагента); ISO₄ — по правилу D10
  (тип с «только I» и «только O»); симметризация полной инверсией; причинность — через игру из носителя
  с точной причинной границей по детерминированным стратегиям с динамическим порядком, если перебор помещается
  (иначе — фиксируется потолок). Калибровка N = 2: класс = 19 (уже в T3.0), и ни один двусторонний
  детерминированный процесс не симметризуем (следствие D6).

## Исходы (из промпта)
- литчек находит тот же результат → ветка сводится к переводу в MH-формализм и контрасту с D6;
- литчек чист, c/d дают общий принцип симметризации → формулировка с характеризацией работающих «зеркал»;
- d: Лугано — единственный симметризуемый класс → фиксируется как есть.

## Прогнозы

**Архитектор:**
- a: понятие «без глобального прошлого» в литературе близко к ISO₃, но симметризация Лугано зеркалом с
  нарушением неравенства явно не выписана — низкая-средняя (риск новизны реальный);
- b: BW16 даёт 1 на Лугано и 0 на зеркале — средняя;
- c: работает не только полная инверсия — средняя-низкая;
- e: для N = 4 симметризация тоже работает — средняя.

**Исполнитель:**

| пункт | прогноз | уверенность |
|---|---|---|
| a | в литературе есть близкое понятие (без глобального прошлого), но явного результата «Лугано/некаузальный процесс без фиксированного прошлого и будущего с нарушением» нет | 0.5 |
| a | найден тот же результат (приоритет чужой) | 0.3 |
| b | BW16 со стратегией «переслать»: Лугано 1, зеркало 0, W* 1/2; G* инвариантна относительно полной инверсии | 0.8 |
| c | работающие g — это ровно «полная инверсия × стабилизатор Лугано в G» (по существу одно зеркало) | 0.5 |
| c | существует работающее g, не сводящееся к полной инверсии композицией с симметрией Лугано | 0.4 |
| c | обмен вход↔выход сам по себе не работает (смесь не в ISO₃) | 0.8 |
| d | допустимых детерминированных функций из 4096 — от 700 до 2000; некаузальных — только образы Лугано под локальными переименованиями и перестановками сторон | 0.6 |
| d | каждый некаузальный детерминированный процесс симметризуем некоторым g | 0.65 |
| e | для N = 4 симметризация полной инверсией работает (∈ ISO₄ и некаузальна) | 0.6 |
