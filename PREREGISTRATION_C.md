# PREREGISTRATION — Stage C (нарушают ли N1 и N2 физически осмысленные процессы)

Запечатано до первого прогона Stage C: `scripts/stage_c.py` ещё не написан, и ни одна SDP не
запускалась. После коммита файл не меняется. Цитаты — SOURCES.md (MH-22…MH-30, B15-8, B15-9);
выводы — D5, D6.

## 0. Сценарий и функционалы

Сценарий Stage B: без настроек, `u, v` маргинализованы, всё бинарное. Статистика (D5):
`p(a,b,x,y) = (1/4) Tr[W̄ (M_{a,x} ⊗ M_{b,y})]`, где `W̄ = Σ_{u,v} W_{u,v}`, а операции —
TS-операции по MH-1 (CP; `Tr_{A_O} Σ_x M_{a,x} = 1` для каждого a;
`(1/2) Tr_{A_I} Σ_a M_{a,x} = (1/2)·1` для каждого x).

Функционалы: по одному представителю каждого класса в «игровой» форме `Σ_{t∈S} p(t) ≤ 1/2`:
N1, N2 (RESULTS, Stage B) и 4 оставшихся непозитивных класса классического замыкания
(«cl3…cl6»). Внутри U2-оболочки (`p(a,b) = p(x,y) = 1/4`) значение от выбора представителя
не зависит. Вне оболочки (режимы с селекцией) используется именно записанная форма, и это
оговаривается.

## 1. Ограниченный класс R (фиксируется здесь, после прогона не меняется)

**R** = { W̄ : W̄ ≥ 0 (MH-24); `Tr W̄ = d_A d_B` (Wcons1); W̄ удовлетворяет vcons1–3 (MH-25,
суммирование по v тривиально) и ucons1–3 (MH-25, суммирование по u тривиально) }.
Это процессы, у которых `u, v` тривиальны или маргинализованы; по MH-27 — класс ISO.
Размерности: `d_A = d_B = 2` (кубиты), затем 3 (кутриты).

Подклассы:
- **P1** — определённый порядок и направление: W̄ ∈ R содержит только члены `A_O B_I` (A→B)
  или только `A_I B_O` (B→A); максимум P1 = максимум по двум вариантам.
- **P2** — процессы OCB (B15-8), лежащие в R: W̄ ∈ R ∩ OCB.
- **P3** — двусторонний «переворот времени» с общим управляющим кубитом: ветвь «вперёд» —
  тождественный канал A_O→B_I (ISO), ветвь «назад» — та же схема с обращёнными входом и
  выходом операций (A_I↔A_O, B_I↔B_O), то есть канал B_O→A_I. В R управляющий кубит нельзя
  приготовить в чистом состоянии или спроецировать: это была бы пред- или постселекция.
  Он маргинализуется, и W̄_P3 = ½(W_f + W_b). Вариант с независимыми контролями содержит
  ветви «вперёд/назад» и «назад/вперёд» с членами `A_O B_O` и `A_I B_I` — проверяется
  принадлежность R (ожидается: не принадлежит).
- **P4** — весь R.

Вне R, только диагностика (не ответ на вопрос стадии):
- **TF-pre** — процессы OCB, статистика условна по u (предселекция), по MH-27 это класс TF;
- **full** — полный класс с пред- и постселекцией, статистика условна по событию (u, v).

## 2. Лемма D6 и её доказательство (записано до расчётов)

(a) Для любого физического процесса (MH-26) `W̄ = Σ_{u,v} W_{u,v} = (1/(d_A d_B))(1 + Σ_{u,v} σ^ISO)`,
так как `Σ_u σ^TS = Σ_u σ^TF = 0`, `Σ_v σ^TB = 0` и `Σ p_0 = 1`. Для R то же следует прямо из
линейных условий (vcons, ucons, Wcons): совместно их допускают только члены `1`, `A_O B_I` и
`A_I B_O`. Эта часть проверяется численно: нуль-пространство буквальных отображений
`_X` на полном базисе при d = 2, 3 должно иметь размерность `1 + 2(d²−1)²`.

(b) Пусть `W̄ = (1/(d_A d_B))(1 + T + S)`, где T — сумма членов `σ_i^{A_O}σ_j^{B_I}`, S — сумма
`σ_i^{A_I}σ_j^{B_O}` (i, j > 0). T и S действуют на разных подсистемах и коммутируют, так что
`W̄ ≥ 0 ⇔ 1 + λ_min(T) + λ_min(S) ≥ 0`. Обе матрицы бесследовые, поэтому λ_min ≤ 0.
Положим `q = −λ_min(T) ∈ [0, 1]`. Тогда `W_1 = (1/(d_Ad_B))(1 + T/q) ≥ 0`,
`W_2 = (1/(d_Ad_B))(1 + S/(1−q)) ≥ 0` и `W̄ = q W_1 + (1−q) W_2`; крайние случаи q = 0, 1 тривиальны.

(c) W_1 — матрица Чоя унитального канала A_O→B_I (частичные следы T нулевые) при максимально
смешанном A_I и сброшенном B_O. Это схема A≼B без пред- и постселекции. С любыми TS-операциями
её статистика удовлетворяет ур. (3), (4) и U2: член T исчезает при `Σ_y` (прямая причинность Боба)
и при `Σ_a` (обратная причинность Алисы). Значит, `p_1 ∈ P_AB` (Def-II), и аналогично `p_2 ∈ P_BA`.

**Следствие:** в сценарии Stage B **ни один процесс формализма MH24 не нарушает ни одной
фасеты основного многогранника TS**, в том числе N1 и N2, при любых размерностях. Классы
замыкания cl3–cl6 при этом нарушаться могут: это фасеты классического замыкания, а не
Def-II, и квантовые схемы с определённым порядком способны выйти за классическое замыкание.

(d) Обратная сторона: N1 и N2 **не выполняются** в режимах с селекцией даже для процессов с
определённым порядком. Схема A≼B с предселекцией входа Алисы |0⟩ (операция Алисы
`(a,i)→(x=a, o=a⊕i)`, тождественный канал A_O→B_I, операция Боба `(b,j)→(y=b⊕j, o_B=b)`)
даёт при условии на u распределение z (`x=a, y=a⊕b`), а z отсекается фасетами N1 и N2 (Stage B.2).

## 3. Калибровки (ворота C.1, провал = стоп)

1. **Формула D5 на примере MH24 (MH-29, с настройками).** Прямое LGYNI — `(2+√2)/4`,
   обратное — `1/2`. exampleW удовлетворяет Wcons1–4 и vcons, **нарушает** ucons
   («requires pre-selection»).
2. **OCB, пример B15-9:** GYNI `5/16(1+1/√2)`, LGYNI то же `+1/4`, до 1e-12.
3. **OCB see-saw на кубитах (B15-8):** GYNI ≥ 0.5694 − 1e-3 и LGYNI ≥ 0.8194 − 1e-3; превышение
   более чем на 1e-3 — тоже провал (либо ошибка, либо опровержение гипотезы B15, требующее разбора).
   Дополнительно W_max из B15 App. C с их инструментами даёт наименьший корень полинома B15-9.
4. **Постселекция достигает максимума.** Процесс без связи между сторонами с пред- и
   постселекцией (приготовление `A_I, B_I`, измерение `A_O, B_O`; операции «исход = вход,
   выход = доход»). Условно на событие (u, v) он даёт `p = ¼·δ[(x,y) = π(a,b)]` для любой
   биекции π. N1 и N2 при этом достигают U2-алгебраического максимума.
5. **Солвер:** известная задача (λ_max случайной эрмитовой матрицы через SDP против numpy,
   до 1e-6) и заведомо недопустимая задача (`Tr W = 4` и `Tr W = 5`): статус должен быть
   infeasible. See-saw падает на любом статусе, кроме optimal.
6. **Классическое замыкание:** максимум N1, N2 и cl3–cl6 на его вершинах = 1/2 (точно, из B.2).

## 4. Подтверждение нарушений
Нарушением считается только значение, превышающее 1/2 более чем на 1e-6 и подтверждённое
прямым пересчётом `p(a,b,x,y)` по найденным W̄ и инструментам в mpmath (50 знаков).
Инструменты и W̄ перед пересчётом проверяются на допустимость (положительность — по
собственным значениям с допуском 1e-9).

## 5. Исходы (из промпта) и моё уточнение
- P3 нарушает, P2 нет, дефазировка убивает нарушение → положительный ответ на вопрос 3 MH24 в R;
- P2 тоже нарушает → N1/N2 свидетельствуют о неопределённом порядке вообще;
- **P4 не нарушает → в R N1 и N2 ничем квантовым не нарушаются; C закрыта отрицательно**;
- C.1 не пройдена → сначала формализм.
Моё уточнение к последнему исходу: если выполнятся D6 и (d), то N1/N2 не нарушаются ничем в
режиме без селекции и нарушаются процессами с определённым порядком в режиме с селекцией.
Тогда N1/N2 не являются свидетелями неопределённости ни в каком режиме формализма MH24 в
этом сценарии, и центральная гипотеза в этой постановке непроверяема, а не только опровергнута.

## 6. Прогнозы

**Прогнозы архитектора** (с его неопределённостью):
- P1 не нарушает — уверенность высокая (контроль);
- P2 не нарушает N1 и N2 — уверенность средняя;
- P3 нарушает хотя бы одно из N1, N2 без постселекции — уверенность низкая (проверяемая гипотеза);
- при дефазировке контроля нарушение P3 исчезает — при условии предыдущего пункта.

**Прогнозы исполнителя:**

| пункт | прогноз | уверенность |
|---|---|---|
| C.0.2 | U2-алгебраический максимум N1 и N2 = 3/4 (биекции (a,b)→(x,y)); на TS = 1/2 | 0.9 |
| C.0.2 | на F, на B и на F∩B максимум N1 и N2 тоже 3/4 (достигают U2-алгебраического) | 0.5 |
| D6(a) | размерность нуль-пространства = 1 + 2(d²−1)² при d = 2 (19) и d = 3 (129) | 0.95 |
| D6(b) | разложение проходит на 200 случайных W̄ ∈ R (d = 2, 3) | 0.97 |
| C.1.1 | MH24: (2+√2)/4 и 1/2; exampleW нарушает ucons | 0.9 |
| C.1.2 | B15: 0.5335 / 0.7835 точно; see-saw OCB: 0.5694 / 0.8194 ± 1e-3 | 0.85 |
| C.1.4 | постселекция даёт 3/4 для N1 и N2 | 0.95 |
| C.1.5 | солвер: известная задача сходится, недопустимая — infeasible | 0.97 |
| C.2 | P1, P2, P3, P4: максимум N1 и N2 = 1/2 (с точностью солвера) на кубитах и кутритах | 0.95 |
| C.2 | P3 с независимыми контролями ∉ R | 0.85 |
| C.2 | P1 (квантовый) нарушает хотя бы один из cl3–cl6 | 0.4 |
| C.2 | TF-pre (OCB с предселекцией): N1 и N2 нарушаются, в т.ч. схемой с определённым порядком (z) | 0.95 |
| C.3 | в R дефазированный и «когерентный» P3 совпадают тождественно (W̄_P3 = ½(W_f + W_b)) | 0.95 |
| итог | исход «P4 не нарушает» | 0.95 |
