# PREREGISTRATION — Stage B.2 (приписываемость, происхождение вершин, чувствительность, замыкание)

Запечатано до первого прогона Stage B.2 (`scripts/stage_b2.py`, `scripts/precision_test.py`
ещё не написаны и не запускались). После коммита файл не редактируется; SHA-256 сверяется
в RESULTS.md. Цитаты: SOURCES.md (MH-15…MH-21, B15-7, вывод D4). Stage B-файл
PREREGISTRATION.md не меняется.

Известно до запечатывания (из Stage B, results/json/stage_b*.json): V(P_AB) = 18,
V(P_TS) = 32, 64 фасеты, 3 класса (положительность + N1 [32] + N2 [16]); z = «x=a, y=a⊕b»
∉ P_TS; классическое замыкание P_TS^cl: 48 вершин, 160 фасет, 7 классов; Def-I (= U0):
64 фасеты, условная форма (8) достигает 227/390 на смеси.

## 0. Точность

Все перечисления — только `cdd.gmp` или `lrs`. Тест: V-представление с вершиной, у которой
координата 1/3, → H → V через cdd.gmp и через lrs; координата должна вернуться ровно
`Fraction(1,3)`. Отрицательный контроль: тот же прогон через float-модуль `cdd` обязан дать
не `Fraction(1,3)` (тест провален). Если float-контроль «проходит» — тест вакуумен, стоп.

## 1. Приписываемость

Приписывание уравнений (D4): F ← {(3), (5)} (прямая причинность), B ← {(4), (6)}
(обратная). Во всех многогранниках п. 1 действует U2: `p(a,b) = p(x,y) = 1/4`.

- `F_AB` = {p ≥ 0, Σp = 1, U2, ур. (3)}, `F_BA` = {…, U2, ур. (5)}, F = conv(V(F_AB) ∪ V(F_BA));
- `B_AB` = {…, U2, ур. (4)}, `B_BA` = {…, U2, ур. (6)}, B = conv(V(B_AB) ∪ V(B_BA));
- TS — основной многогранник Stage B.

Функционал f (фасета TS, `f·p ≤ f0`) **нарушается на X**, если `max_{V(X)} f·p > f0` (точно).
Классификация:
- **смешанное** — нарушается и на F, и на B;
- **направленное вперёд** — выполняется на F, нарушается на B (приписываемо прямому направлению);
- **направленное назад** — выполняется на B, нарушается на F;
- **общее** — выполняется на обоих.
(Промпт формулирует «направленное» как «нарушается только на одном»; здесь то же самое,
с явным указанием, на каком.) Обмен сторон и перевороты сохраняют F и B (проверяется),
TR меняет их местами, поэтому смешанность и общность — свойства класса по G, а направленные
члены класса идут парами «вперёд/назад». Отчёт — по каждому члену и по классу.

F∩B: H-представление F и B (cdd.gmp) → объединение неравенств → вершины (cdd.gmp),
сравнение с V(TS). Если TS ⊊ F∩B: предъявить вершину F∩B вне TS и фасету TS, которая её отсекает.

Калибровка теста (провал = стоп, тест вакуумен):
- K_fwd (48 фасет B15 в прямом чтении) — все выполняются на F;
- хотя бы один элемент K_fwd нарушается на B (есть «направленный вперёд»), и его образ при TR
  — «направленный назад»;
- положительность — «общая».
GYNI в прямом чтении (контроль из промпта) докладывается, но в калибровку не входит: при U2
GYNI ≡ обратное GYNI (D3), поэтому оно обязано быть «общим». Отклонение от промпта.

## 2. Происхождение вершин

Факт (из кода Stage B, не прогноз): вершины Stage B получены `P.vertices_cdd` — это крайние
точки H-описания (ур. (3), (4) [или (5), (6)] + положительность + нормировка + U2) в cdd.gmp.
Детерминированные функции использовались только в перекрёстной проверке. Независимый
пересчёт: те же H-описания → вершины через **lrs** (H→V); сравнение множеств с Stage B.
Если множества различаются — Stage B пересчитывается.

## 3. Чувствительность к равномерности

- U2: `p(a,b) = p(x,y) = 1/4` (Stage B); U1: только `p(a,b) = 1/4`; U0: без равномерности.
- Группа для U1/U0: подгруппа G, состоящая из автоморфизмов V(TS_U).
- Класс N из U2 **переживает U1**, если найдётся фасета TS_U1, чья U2-каноническая форма
  (проекция на aff(TS_U2), примитивный целый вектор, lexmin по G) совпадает с канонической
  формой N. Не переживший класс помечается «следствие допущения U2».
- Для переживших классов — тест приписываемости в U1-версиях F и B (F, B с U1 вместо U2).
- U0: проверить, что условная форма (8) нарушается (ожидается ровно 227/390 при том же зерне).
  Дополнительно: условная форма (10) в U1 (нелинейна, т.к. p(x,y) не фиксирован).

## 4. Замыкание классических схем

Проверить каждую из 48 вершин P_TS^cl против H-описания Stage B (все 64 фасеты TS и равенства
aff). Если какая-то вне — предъявить и указать нарушенное неравенство/равенство; различить
«(3)–(6) не необходимы» и «замыкание построено с ошибкой» проверкой: точка μ → схема с
конкретными размерностями → прямой пересчёт распределения биекциями → ур. (3)–(6).
Для 7 классов P_TS^cl — тест приписываемости п. 1 (те же F, B: по B15-7 временно-прямые
корреляции порядка A≺B — детерминированные, так что H-вариант F_AB совпадает с классически
реализуемым, и отдельного «F^cl» не нужно).

«Подтверждён в обоих построениях»: канонический вид класса N (U2) совпадает с каноническим
видом какого-либо класса фасет P_TS^cl (основное определение). Слабое определение
(докладывается отдельно): в P_TS^cl есть хотя бы один смешанный класс вне K.

## 5. Исходы (из промпта) и уточнения

- **Кандидат:** есть класс вне K, смешанный, переживший U1 и подтверждённый в обоих построениях
  (основное определение) → литчек и письмо авторам до заявлений о новизне; Stage C — архитектор.
- **Отрицательный:** смешанных классов нет → вопрос 1 в минимальном сценарии закрыт
  отрицательно в смысле статьи.
- **Ошибка построения** (п. 2 или 4) → исправление, повтор п. 1, пересмотр выводов Stage B.
- Промежуточные случаи (смешанный есть, но не пережил U1 или не подтверждён в замыкании)
  докладываются как есть, с указанием, какое условие не выполнено.

## 6. Прогнозы

**Прогноз архитектора** (с его неопределённостью):
- TS строго меньше F∩B — уверенность высокая (из-за точки x = a, y = a⊕b, но только если она лежит в F∩B);
- хотя бы один из двух новых классов Stage B смешанный — уверенность средняя;
- хотя бы один новый класс переживает переход U2 → U1 — уверенность средняя.

**Прогноз исполнителя:**

| пункт | прогноз | уверенность |
|---|---|---|
| 0 | точный путь даёт ровно 1/3, float-контроль проваливается | 0.99 |
| 1 | z ∈ F (через F_AB), z ∈ B (через B_BA), z ∈ F∩B, TS ⊊ F∩B | 0.95 |
| 1 | **оба** новых класса (N1, N2) смешанные: у каждого есть члены, которые нарушает z ∈ F∩B | 0.9 |
| 1 | положительность — общая; GYNI — общее (D3), т.е. контроль промпта «на B может нарушаться» не сработает | 0.95 |
| 1 | хотя бы один элемент K_fwd нарушается на B (калибровка проходит) | 0.8 |
| 1 | z — вершина F∩B | 0.7 |
| 1 | число вершин F∩B | 40 (диапазон 33–300) |
| 2 | lrs-пересчёт даёт те же 18+18 вершин; 14 из 18 имеют координаты 1/8, 4 — координаты 1/4; вершин с координатами 0/1 нет вообще (при U2 любая точка имеет p(a,b)=1/4, значит координаты ≤ 1/4) | 0.97 |
| 3 | U1: TR — не автоморфизм, группа порядка 32 | 0.85 |
| 3 | хотя бы один из N1, N2 переживает U1 | 0.6 |
| 3 | условная форма (10) нарушается смесью в U1 | 0.6 |
| 3 | U0 воспроизводит 227/390 | 0.97 |
| 4 | все 48 вершин P_TS^cl внутри TS (Def-II) | 0.95 |
| 4 | рост числа вершин: P_TS^cl — строго меньший многогранник внутри TS, его новые вершины дробные (знаменатели кратны 3, т.е. 12 в координатах p), не вершины TS | 0.8 |
| 4 | хотя бы один класс P_TS^cl смешанный | 0.95; все 6 новых смешанные — 0.5 |
| 4 | хотя бы один из N1, N2 — класс фасет P_TS^cl | 0.5 |
| итог | исход «кандидат» (все три условия) | 0.35 |

Рассуждение к п. 1: в F_AB (ур. (3)) нужно b ⊥ (a, x) — у z x = a, b равномерен и независим;
в B_BA (ур. (6)) нужно y ⊥ (a, x) — у z y = a⊕b при независимом равномерном b. Смешанность
класс-инвариантна: обмен и перевороты сохраняют F и B, TR меняет их местами.
