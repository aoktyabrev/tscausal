# PREREGISTRATION — Stage B (TSCAUSAL stage 0.1)

Запечатано до первого прогона Stage B. После коммита этот файл не редактируется;
SHA-256 выносится в RESULTS.md, а `scripts/make_results.py` сверяет его при каждой
генерации отчёта. Цитаты и номера уравнений — в SOURCES.md (метки MH-*, B15-*, D1–D3).
На момент запечатывания выполнен только Stage A (results/json/stage_a.json); ни одна
вершина, фасета или максимум сценария Stage B ещё не вычислялись.

## 1. Сценарий (основное определение, Def-II)

Две стороны, всё бинарное, настроек нет (`N_α = N_β = 1`), `u, v` маргинализованы.
Координаты: совместное распределение `p(a,b,x,y)`, 16 чисел, `a,b` — доходы, `x,y` — исходы.

- `P_AB` = { p ≥ 0, Σp = 1, D1: `p(a,b)=1/4`, `p(x,y)=1/4`,
  ур. (3): `p(a,b,x) = p(a,x)/2`, ур. (4): `p(b,x,y) = p(b,y)/2` }.
- `P_BA` = { p ≥ 0, Σp = 1, D1, ур. (5): `p(a,b,y) = p(b,y)/2`, ур. (6): `p(a,x,y) = p(a,x)/2` }.
- `P_TS` = conv(`P_AB` ∪ `P_BA`) — ур. (7).

Вершины: точное (GMP) вершинное перечисление `P_AB` и `P_BA`, объединение.
Перекрёстная проверка «детерминированных вершин»: распределения детерминированных
классических TS-схем (MH-1 для классических операций = двоякостохастичность; её крайние
точки — биекции (доход, вход) ↔ (исход, выход)); схема A ≼ B: равномерный начальный
вход и анцилла → биекция Алисы → биекция-канал → биекция Боба; `a, b` равномерны.
Размерности: вход/выход 2, анцилла 1, 2, 4.

Контроль чувствительности (Def-I): те же ур. (3)–(6) **без** D1. Считается и
докладывается, но выводы делаются по Def-II.

## 2. Известные неравенства и множество K

Функционалы на `p(a,b,x,y)` (D2, D3):
- GYNI = обратное GYNI: `Σ_{x=b, y=a} p ≤ 1/2`;
- LGYNI_fwd: `Σ p [a(x⊕b)=0 ∧ b(y⊕a)=0] ≤ 3/4`;
- LGYNI_bwd: `Σ p [x(a⊕y)=0 ∧ y(b⊕x)=0] ≤ 3/4`.

K («сводимое к известным») = 48 фасет многогранника B15 в прямом чтении (входы = доходы,
выходы = исходы) ∪ 48 фасет в обратном чтении (входы = исходы, выходы = доходы), каждая
записанная как функционал на `p(a,b,x,y)` через D1. Это GYNI, LGYNI, их обращения и
положительность со всеми переименованиями B15 (включая условные перевороты выхода).
Фасета `P_TS` «известна», если её канонический вид (проекция на aff(`P_TS`), примитивный
целый вектор) совпадает с каноническим видом элемента K.

## 3. Группа и канонизация

G = ⟨ обмен сторон (a,b,x,y)→(b,a,y,x); обращение времени (a,b,x,y)→(x,y,a,b);
перевороты a, b, x, y ⟩. Класс = орбита G канонического вида. G' = G без обращения
времени (⟨обмен, перевороты⟩). Новый класс называется **направленным**, если для его
представителя f обращение TR(f) не G'-эквивалентно f.

Антивакуумные тесты (обязательны, провал любого = стоп):
- каждый генератор — нетождественная перестановка координат и автоморфизм множества вершин;
- каждый генератор хотя бы одно известное неравенство (с переименованиями) переводит в
  другое, и образ имеет тот же максимум на вершинах;
- отрицательный контроль: отображение, не являющееся симметрией (условный переворот
  x→x⊕a, обращение времени только у Алисы, случайная перестановка координат), должно быть
  отвергнуто проверкой автоморфизма;
- канонизатор уже откалиброван в Stage A (3 класса по 16 на B15).

## 4. Ворота Stage B (точные, без допусков)

На вершинах `P_TS`: max GYNI = 1/2, max обратного GYNI = 1/2, max LGYNI_fwd = 3/4,
max LGYNI_bwd = 3/4. Любое расхождение — ошибка в определении вершин, стоп.
Перечисление фасет: cdd.gmp и lrs, совпадение множеств после проекции; расхождение — стоп.
Каждая выданная фасета проверяется на валидность и фасетность (ранг насыщающих вершин).
Вершинная перекрёстная проверка: точка схемы вне `P_AB` — стоп; вершина `P_AB`, не
достигнутая схемами до анциллы 4, — не стоп, а докладывается (основным остаётся
определение через ур. (3)–(7), MH-13).

## 5. Исходы (зафиксированы заранее)

- **Отрицательный:** все фасеты `P_TS` в K. Вопрос 1 закрыт в этом сценарии отрицательно.
- **Положительный:** есть класс вне K, и он направленный. Кандидат в Stage C (запускает архитектор).
- **Новый, но ненаправленный:** есть класс вне K, но каждый такой класс G'-эквивалентен
  своему обращению. Критерий промпта для Stage C формально не выполнен; решение за архитектором.
- **Потолок:** перечисление не завершилось — фиксируется числом, частичный перебор не выдаётся за полный.

## 6. Прогнозы

**Прогноз архитектора:** в сценарии без настроек новых классов фасет не будет, всё уложится в четыре известных неравенства.

**Прогноз исполнителя** (до вычислений; рассуждение ниже):

| величина | точечный | диапазон |
|---|---|---|
| вершин `P_AB` (= `P_BA`) | 12 | 12–60 |
| вершин `P_TS` | 20 | 20–120 |
| аффинная размерность `P_TS` | 9 | 7–9 |
| фасет `P_TS` | 40 | 10–500 |
| классов по G (с положительностью) | 5 | 3–20 |
| исход | новый класс вне K существует (P ≈ 0.9); направленный ли — P ≈ 0.5 |

Рассуждение. (i) Равномерные распределения на гиперплоскостях `λ·(a,b,x,y) = c` над
GF(2), совместимые с Def-II для A ≼ B, дают 6 допустимых λ; из них вершинами, по-видимому,
являются 4 сигнальных типа × 2 значения c = 8, плюс 4 несигнальные точки `x=a⊕c1, y=b⊕c2`
(общие для обоих порядков): 12 вершин, `P_TS`: 12+12−4 = 20. (ii) Точка
«`x = a`, `y = a⊕b`, `(a,b)` равномерно» лежит в прямом многограннике B15 (x зависит только
от a) и в обратном (a = x, b = y⊕x), маргиналы равномерны, но её носитель не содержит ни
одной точки `P_AB` или `P_BA` (в `P_AB` x должен быть независим от `(b,y)`, а здесь
x = y⊕b). Если это верно, `P_TS` строго меньше пересечения прямого и обратного
многогранников B15, и у `P_TS` обязана быть фасета вне K. Эта точка инвариантна
относительно обращения времени, поэтому отделяющая её фасета может оказаться
ненаправленной — отсюда 0.5. (iii) Для Def-I предсказываю, что условная форма (8)
нарушается смесью порядков с неравномерными доходами (значение > 1/2), то есть Def-I
несовместимо с собственными неравенствами MH24.
