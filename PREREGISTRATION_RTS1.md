# PREREGISTRATION — RTS stage 1 (финальный скан по размерности)

Запечатано до первого прогона стадии 1. Код скана не написан. Числа ниже — прогнозы, а не результаты.
Наследуется всё из `PREREGISTRATION_RTS0.md` (перевод D12, класс ISO, определение ОН); здесь фиксируются
только новые определения и прогнозы.

## Определения (фиксируются здесь)

- **Сценарий и функционал** — те же: D12, 𝒯 по RTW21 (коэффициенты — `scripts/rts.py`), операции свободные
  (TS-условия: Σ_x Tr A_{a|x} = 3/2·d_A, Σ_z Tr C_{c|z} = 3·d_C, Tr F_b = d_B1·d_B2/4).
- **Процесс:** ω = ω₁⊗ω₂ + Δ на A⊗B1⊗B2⊗C, Δ ∈ Anti(A⊗B1)⊗Anti(B2⊗C); ISO ⇔ маргиналы на (A,C) и на
  (B1,B2) максимально смешаны; ОН выполняется точно по построению Δ.
- **Очистка финальной точки (пофакторный шум, из RTS 0):** ω(q) = w₁(q)⊗w₂(q) + (1−q)Δ, где
  w_i(q) = (1−q)w_i + q·I/n_i, q — минимальное из деления отрезка, при котором ω ≥ 0 (сертификат — Cholesky).
  ISO и ОН при этом точные. **Отчётное число — только очищенное 𝒯.** Белый шум I/N не используется:
  он не является произведением и ломает ОН на своём же уровне.
- **Успешный старт** — все SDP-шаги вернули статус optimal или optimal_inaccurate и финальная точка
  прошла очистку. Отказ солвера на любом шаге старта = упавший старт, он отбрасывается целиком и считается.
- **Тёплый старт** — вложение очищенной точки размерности d в размерность d′ > d: ω ↦ ω ⊕ (максимально
  смешанное на дополнении) с сохранением ISO-маргиналов, операции дополняются нулями на дополнении и
  перенормируются под TS-условия. Тёплые и холодные старты в отчёте разделены; тёплый старт без холодного
  той же размерности не засчитывается.
- **Потолок по ресурсам** фиксируется числами: размер PSD-блока N×N, пиковая память, время одной итерации
  see-saw и одного SDP-шага. Экстраполяция вместо счёта не допускается.

## Прогнозы

**Архитектор:**
- при (8,8,8,8) значение заметно поднимется над 6.87 — уверенность ~50%. Пометка: в RTS 0 прогноз в ту же
  сторону («HW переносится», ~55%) не сбылся.

**Исполнитель:**

| пункт | прогноз | уверенность |
|---|---|---|
| S.1 | (2,2,2,2): лучшее очищенное 𝒯 = 4+2√2 = 6.828427, как в RTS 0 | 0.9 |
| S.1 | (4,4,4,4), холодные старты: лучшее очищенное 𝒯 в [6.82, 6.95] | 0.7 |
| S.1 | (4,4,4,4): холодный старт не превзойдёт тёплый/HW-старт (6.869154) более чем на 0.05 | 0.65 |
| S.1 | (6,6,6,6): прибавка к (4,4,4,4) меньше 0.1 | 0.6 |
| S.1 | плато: (4,4,4,4) и (6,6,6,6) в пределах 0.05 друг от друга и ниже 7.6605 | 0.55 |
| S.1 | ни при одной размерности 𝒯 не превысит вещественную границу Renou 7.6605 | 0.85 |
| S.1 | ни при одной размерности 𝒯 не достигнет 6√2 = 8.485281 | 0.9 |
| S.1 | (8,8,8,8) не помещается в бюджет суток; будет зафиксирован потолок числами | 0.85 |
| S.2 | верхняя оценка при свободных операциях **не** будет получена | 0.8 |
| S.2 | если редукция по симметрии и сработает, то уменьшит блок не более чем вчетверо | 0.6 |
| итог | ветка закрывается исходом «плато ниже 7.6605», то есть свидетельством в пользу того, что временная симметрия режет лазейку ОН | 0.55 |

Рассуждение. Вклад в 𝒯 в модели HW несут четыре ребитовых шаблона, и два из них ISO запрещает уже при
размерности 4 на сторону; увеличение размерности добавляет не новые типы членов, а лишь кратность, поэтому
жду насыщение, а не рост к 8.485. Главный риск прогноза: при d ≥ 6 появляются ISO-допустимые конструкции,
не сводящиеся к ребитовой, и тогда возможен скачок. Второй риск — не вычислительный, а модельный: плато
может оказаться артефактом see-saw, который при росте размерности всё хуже находит глобальный максимум;
против этого — доля упавших стартов и разброс значений по стартам, они пойдут в отчёт.

Ожидаемый потолок ресурсов (прогноз, не результат): PSD-блок растёт как (d²)², то есть 256 при d = 4,
1296 при d = 6, 4096 при d = 8; проекция SCS на конус стоит O(N³) на итерацию, поэтому (8,8,8,8) ожидаю
недосягаемым, а (6,6,6,6) — на грани.
