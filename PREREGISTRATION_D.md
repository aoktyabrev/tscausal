# PREREGISTRATION — Stage D (квантово-классический разрыв на cl3–cl6)

Запечатано до первого прогона Stage D (`scripts/stage_d.py` не написан и не запускался).
Файл после коммита не меняется. Цитаты — SOURCES.md; выводы D7, D8 записаны ниже до расчётов.
Литчек D.0.3 выполняется параллельно субагентом, его результаты в этот файл не входят.

## 0. Объект

Сценарий Stage B (без настроек, `u, v` маргинализованы). Функционалы cl3–cl6 — ровно в той
форме, как в RESULTS.md (Stage C, C.0): целые веса на `p(a,b,x,y)`, граница `rhs`. Разрыв —
превышение `rhs` квантовыми TS-процессами с определённым порядком (по лемме D6 других в R нет).

## 1. Вывод D7: полнота классического замыкания (доказательство, до расчётов)

Классический TS-процесс A≼B без селекции (по D6) — это диагональный унитальный канал A_O→B_I
(двоякостохастическая матрица) при равномерном A_I. Классические TS-операции (MH-1, диагональный
случай): Алиса — неотрицательные `M_{a,x}(o|i)` с `Σ_{x,o} M = 1` для каждого (a, i) и
`Σ_{a,i} M = 1` для каждого (x, o) (N_a = N_x = 2). Поскольку `i` равномерно и ни с чем не связано,
Алиса эквивалентна набору распределений `σ_{a,x}(o) = (1/d) Σ_i M_{a,x}(o|i)` с условиями
`Σ_{x,o} σ_{a,x}(o) = 1` и `Σ_a σ_{a,x}(o) = 1/d`. Канал поглощается мерой Боба.
Положим `q_o(a|x) = d·σ_{a,x}(o)`: это условное распределение a при данном x для каждого символа o.
Условие Алисы: `E_o[q_o(a|0) + q_o(a|1)] = 1` при равномерном o. Аналогично у Боба —
`r_o(y|b)` с `E_o[r_o(y|0) + r_o(y|1)] = 1`. Отсюда
`p(a,b,x,y) = ¼ E_λ[q_λ(a|x) r_λ(y|b)]` — локальная модель со скрытой переменной λ, откликами
«a от x» и «y от b» и **усреднёнными** ограничениями на маргиналы. Каждую пару откликов можно
разложить на детерминированные, и линейные ограничения сохраняются в среднем. Поэтому при
произвольных размерностях множество — это образ многогранника M из Stage B (типы tA, tB,
`E|tA| = E|tB| = 1`). Вершины M рациональны; знаменатели уже найдены: N ≤ 4 (B.2, п. 4, явные схемы).
Следовательно, **замыкание полное, классическая граница cl3–cl6 точна при любых размерностях**.
Численный контроль: классический see-saw (диагональные σ, F) при d = 5, 6.

## 2. Вывод D8: сведение к Белловскому сценарию (до расчётов)

Квантовый TS-процесс A≼B без селекции: `W = (1/d²)(1 + T)`, где T — чоевская матрица
унитального канала Λ: A_O→B_I, A_I максимально смешано, B_O сброшено (D6). Для TS-операций
положим `σ_{a,x} = (1/d) Tr_{A_I} M_{a,x}`. MH-1 даёт `Σ_x Tr σ_{a,x} = 1` и `Σ_a σ_{a,x} = 1/d`.
Мера Боба: `F_{b,y} = Tr_{B_O} M_{b,y}` с условиями `Σ_y F_{b,y} = 1` и `Σ_b Tr F_{b,y} = d`;
канал поглощается: `Λ†(F)` удовлетворяет тем же условиям, так как Λ унитален и сохраняет след.
Тогда `p = ¼ Tr[σ_{a,x}^T F_{b,y}]` (транспонирование — из конвенции CJ; проверяется численно).
Обратно: `M_{a,x} = 1 ⊗ σ_{a,x}`, `M_{b,y} = F_{b,y} ⊗ 1/d` и тождественный канал реализуют
любую пару (σ, F). С `A_{a|x} = d σ_{a,x}` и нормированным максимально запутанным `|φ_d⟩`:
`p = ¼ ⟨φ_d| A_{a|x}^{(T)} ⊗ F_{y|b} |φ_d⟩`.

**Квантовое TS-множество A≼B — это Белловские корреляции на |φ_d⟩ со входами (x, b), выходами
(a, y) и условиями на маргиналы `P(a|0) + P(a|1) = 1`, `P(y|0) + P(y|1) = 1`.** Def-II `P_AB` —
соответствующее NS-множество с теми же условиями (ур. (3), (4) — это отсутствие сигнала в ролях
Белла), классическое замыкание — локальное множество. Ожидаемое следствие: 8 «нереализуемых»
вершин P_AB — PR-ящики, а 3+√2 — граница Цирельсона в этой нормировке.

## 3. Методы

- **D.0.1:** для каждого cl_k и каждого порядка — проверить, совпадает ли cl_k на aff(P_order) с
  α·CHSH_j + β для одного из 8 переименований CHSH в ролях Белла этого порядка (точная линейная
  алгебра). Связи по G: cl3–cl6 — разные G-классы (Stage C), дополнительно докладывается это.
- **D.0.2:** свидетели Stage C → (σ, F) → читаемый вид. Минимальный свидетель — аналитический
  кубитный, построенный по CHSH-оптимальной стратегии; значение проверяется символьно (sympy).
- **D.1:** доказательство D7 + численный контроль d = 5, 6.
- **D.2:** прямой сценарий: операции только с прямой нормировкой, A≺B, `p(a,b) = 1/4`. Классический
  многогранник — детерминированные вершины B15 (A≺B) в ролях «входы = доходы». Функционалы
  переносятся **буквально** (те же веса на p(a,b,x,y)); вариант с фильтром `p(x,y) = 1/4` —
  отдельно. Квантовый максимум — see-saw по процессам OCB A≺B с операциями только с прямой
  нормировкой. Калибровка: классическая схема «передать a по каналу» реализует
  максимизирующую вершину.
- **D.3:** NPA уровня 1+AB для Белловского сценария D8 (все состояния — верхняя оценка) с
  линейными условиями на маргиналы. Калибровки: CHSH без условий → (2+√2)/4 (цитата NPA —
  от субагента, иначе «не подтверждено»); LP по локальному множеству с условиями → классическая
  граница; LP по NS-множеству с условиями → максимум Def-II; результат NPA обязан лежать между ними.
- **D.4:** see-saw в сведённой форме (σ, F) — эквивалентной по D8, намного дешевле TS-формы — при
  d = 2, 3, 4, 50 стартов; лучшие решения переводятся обратно в TS-форму (W, инструменты) и
  пересчитываются в mpmath. Вещественный вариант: σ, F вещественные симметричные; контроль —
  вещественные классические (диагональные) дают классическую границу.
- **D.5:** Алиса «повёрнуто-классическая»: `M_{a,x} = (U⊗V) C_{a,x} (U⊗V)†`. Поворот поглощается
  каналом и мерой Боба, так что достаточно диагональных σ при произвольных F и канале.
  Смеси повёрнутых классических операций дают совместно измеримые `A_{a|x}` (смесь совместно
  измеримых семейств совместно измерима), поэтому ожидается отсутствие разрыва. Проверка —
  see-saw с диагональными σ.

## 4. Исходы (из промпта)
- D.1 доказана, D.2 без разрыва, D.3 совпала → граница типа Цирельсона для TS-схем с определённым
  порядком; дальше литчек новизны и расширение на настройки;
- D.2 показывает разрыв → находка не про время;
- D.1 не доказана → выводы о разрыве условные;
- D.3 выше 3+√2 → зазор фиксируется.
Моё уточнение: если подтвердится D8, «граница типа Цирельсона» — это буквально граница Цирельсона
для CHSH в переобозначенных ролях. Временная симметрия (обратная нормировка) превращает канал
в ресурс без сигнала, эквивалентный максимально запутанному состоянию. Тогда разрыв — это
нелокальность Белла, а не новый эффект, и вопрос новизны сводится к тому, известно ли это
сведение (темпоральный CHSH — Fritz 2010, Brukner et al. 2004 — литчек).

## 5. Прогнозы

**Архитектор** (с его неопределённостью):
- D.1: замыкание полное, граница 4 доказывается — уверенность средняя-высокая;
- D.2: в чисто прямом сценарии разрыва нет — средняя (главная проверяемая гипотеза);
- D.3: 3+√2 — оптимум по всем размерностям — средняя;
- D.4.1: на d = 3, 4 значение не растёт — средняя-низкая;
- D.4.2: вещественная КМ достигает 3+√2 — средняя (по виду числа).

**Исполнитель:**

| пункт | прогноз | уверенность |
|---|---|---|
| D8 | сведение TS ↔ (σ, F) ↔ Белл численно точно (≤ 1e-10) на свидетелях Stage C и случайных точках | 0.95 |
| D8 | 8 вершин Def-II P_AB вне классического замыкания — PR-ящики в ролях Белла | 0.9 |
| D.0.1 | каждый cl_k на порядке, где он нарушается, аффинно равен переименованию CHSH | 0.7 |
| D.0.2 | минимальный свидетель: тождественный канал, Алиса готовит BB84-состояние (базис = x, бит = a), Боб меряет в повёрнутом на 45° базисе по b; значение ровно 3+√2 (cl3) | 0.75 |
| D.1 | D7 верно; классический see-saw при d = 5, 6 не превышает границу | 0.97 |
| D.2 | разрыва нет: квантовый максимум = классическому = максимуму прямого многогранника | 0.95 |
| D.3 | NPA 1+AB = 3+√2 (cl3, cl4, cl6) и 9/2+(√2−1) (cl5) до 1e-6; CHSH-калибровка (2+√2)/4 | 0.85 |
| D.4.1 | d = 3, 4: максимум не растёт | 0.9 |
| D.4.2 | вещественная КМ достигает 3+√2; вещественная классика — 4 | 0.9 |
| D.5 | повёрнуто-классическая Алиса: максимум = классической границе | 0.95 |
| итог | исход «граница типа Цирельсона» (все три условия), с оговоркой, что это CHSH в других ролях | 0.8 |
